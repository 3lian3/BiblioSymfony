{% extends 'base.html.twig' %}

{% block title %}Modification{% endblock %}

{% block myTitle %}
{% if isModification %}
    Modification "{{ book.title }}"
{% else %}
    Ajout d'un livre
{% endif %}
{% endblock %}

{% block body %}

{{ form_start(form) }}
    <div class="row">
        <div class="col-4  p-5 b-2">
            <img src="{{ asset('img/' ~ book.img) }}" alt="{{ book.title }}" style="width:22%; padding-bottom:40px">
            <div>{{ form_row(form.img) }}</div>
        </div>
        <div class="col-8 p-5">
            <div>{{ form_row(form.title, {"attr" : {'class':'fw-bold'}}) }}</div>
            <div>{{ form_row(form.dateOfPublication) }}</div>
            <div>{{ form_row(form.category) }}</div>
            <div class="form-group row">
            <div class="col-sm-10">
                {{ form_row(form.author) }}
            </div>
            <div class="col-sm-2">
                <a href="{{ path('admin_author_new')}}" class="btn btn-primary">Ajouter auteur</a>
            </div>
            </div>
        <div class="form-group row">
            <div class="col-sm-10">
                {{ form_row(form.editor) }}
            </div>
            <div class="col-sm-2">
                <a href="{{ path("admin_editor_new")}}" class="btn btn-primary">Ajouter éditeur</a>
            </div>
        </div>
    </div>
    <input type="submit" class="btn btn-primary ms-5" style="width:150px" value="Valider"/>
{{ form_end(form) }}

{% endblock %}